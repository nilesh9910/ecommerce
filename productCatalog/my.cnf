[mysqld]
server-id = 1                     # Unique server ID (required for replication)
log-bin = mysql-bin               # Enable binary logging
binlog-format = ROW               # Use ROW format (required by Debezium)
binlog-do-db = ecommerce          # Capture changes only from this database